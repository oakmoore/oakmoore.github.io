const puppeteer = require('puppeteer');(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setViewport({ width: 390, height: 844 }); await page.goto('file:///' + __dirname.replace(/\\/g, '/') + '/product.html'); const overflowElements = await page.evaluate(() => { const elements = document.querySelectorAll('*'); const overflow = []; elements.forEach(el => { const rect = el.getBoundingClientRect(); if (rect.right > 390) overflow.push({ tag: el.tagName, className: el.className, right: rect.right, width: rect.width }); }); return overflow; }); console.log(JSON.stringify(overflowElements, null, 2)); await browser.close(); })();
