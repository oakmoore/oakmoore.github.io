const puppeteer = require('puppeteer');(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setViewport({ width: 390, height: 844 }); await page.goto('file:///' + __dirname.replace(/\\/g, '/') + '/product.html'); const data = await page.evaluate(() => { return { titleWidth: document.querySelector('.product-title')?.getBoundingClientRect().width, paddingOverview: window.getComputedStyle(document.querySelector('.product-overview')).padding, containerWidth: document.querySelector('.product-container')?.getBoundingClientRect().width, footerWidth: document.querySelector('.main-footer')?.getBoundingClientRect().width }; }); console.log(data); await browser.close(); })();
